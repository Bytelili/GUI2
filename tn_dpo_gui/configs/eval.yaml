main_project:
  enabled: true
  root_config: ../../config.yaml

data:
  pairs_path: auto
  splits:
    - eval

checkpoints:
  ranker_path: auto
  gate_path: auto

evaluation:
  batch_size: 16

output:
  report_path: auto

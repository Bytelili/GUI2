# Synthetic PAPO v4 fixture

The v4 tests generate tiny strict-task JSONL files and fake image bytes inside a temporary directory. All user IDs, intents, paths, candidates, and provenance are synthetic; no real task, candidate, image, or review data is committed.
